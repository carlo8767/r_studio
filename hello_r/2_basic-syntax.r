# Assignment Operators
# Assign the number 10 to the variable 'x'
x <- 10

# Comments
# This is a comment

# Data Types
x <- 5 # number
name <- "John" # string
is_student <- TRUE # boolean

# Vectors
numbers <- c(1, 2, 3, 4, 5) # See arrays in JS

# Functions
my_function <- function(x) { # See functions in JS
  x * 2
}

# Installing packages
install.packages("ggplot2") # Advanced R package for creating visualizations
library(ggplot2) # Load ggplot2 package

# Data Frames
df <- data.frame( # See objects in JS
  Name = c("John", "Alice", "Bob"),
  Age = c(25, 30, 35),
)

# Indexing
numbers <- c(1, 2, 3, 4, 5)
numbers[3] # Access the third element of the vector data structure

# Control structures
x <- 10
# Check if the number is greater than 5
if (x > 5) {
  print("x is greater than 5")
} else {
  print("x is less than or equal to 5")
}

# Help and Documentation
help(ggplot2)
?ggplot2
